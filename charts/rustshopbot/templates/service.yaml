apiVersion: v1
kind: Service
metadata:
  name: {{ include "rustshopbot.fullname" . }}
  labels:
    {{- include "rustshopbot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rustshopbot.selectorLabels" . | nindent 4 }}
